{
  "name": "row-to-object",
  "version": "0.4.0",
  "description": "Convert a row (from csv, excel) to a structured object and validate the object",
  "main": "index.js",
  "scripts": {
    "test": "node --expose-internals ./node_modules/.bin/mocha $(find test -name '*spec.js') --exit --recursive"
  },
  "author": "Jaap van der Kreeft",
  "license": "MIT",
  "dependencies": {
    "error-types": "git+https://github.com/toxus/error-types.git",
    "jexl": "github:toxus/Jexl",
    "lodash": "^4.17.15",
    "parse-full-name": "github:toxus/parse-full-name"
  },
  "devDependencies": {
    "mocha": "^6.2.0",
    "chai": "^4.2.0"
  }
}
